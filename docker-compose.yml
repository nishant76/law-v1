version: '3.8'

services:
  # FastAPI backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: superadvocate-backend
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=development
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=redis://redis:6379/0
      - USE_AZURE_OPENAI=${USE_AZURE_OPENAI:-false}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_ORG_ID=${OPENAI_ORG_ID}
      - AZURE_OPENAI_API_KEY=${AZURE_OPENAI_API_KEY}
      - AZURE_OPENAI_ENDPOINT=${AZURE_OPENAI_ENDPOINT}
      - AZURE_OPENAI_API_VERSION=2024-10-21
      - GPT4O_DEPLOYMENT=${GPT4O_DEPLOYMENT:-gpt-4o}
      - GPT4O_MINI_DEPLOYMENT=${GPT4O_MINI_DEPLOYMENT:-gpt-4o-mini}
      - EMBEDDING_DEPLOYMENT=${EMBEDDING_DEPLOYMENT:-text-embedding-ada-002}
      - AZURE_SEARCH_ENDPOINT=${AZURE_SEARCH_ENDPOINT}
      - AZURE_SEARCH_KEY=${AZURE_SEARCH_KEY}
      - BLOB_CONNECTION_STRING=${BLOB_CONNECTION_STRING}
      - SENDGRID_API_KEY=${SENDGRID_API_KEY}
      - WHATSAPP_API_TOKEN=${WHATSAPP_API_TOKEN}
      - WHATSAPP_PHONE_NUMBER_ID=${WHATSAPP_PHONE_NUMBER_ID}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-dev-secret-key-change-in-production}
      - JWT_ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=60
      - REFRESH_TOKEN_EXPIRE_DAYS=30
      - APPLICATIONINSIGHTS_CONNECTION_STRING=${APPLICATIONINSIGHTS_CONNECTION_STRING}
      # Run Chrome headlessly inside the container (no display)
      - ECOURTS_HEADLESS=1
    volumes:
      - .:/app
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - superadvocate-network

  # PostgreSQL database (local dev only — production uses Neon/Cloud SQL)
  postgres:
    image: postgres:16.1-alpine
    container_name: superadvocate-postgres
    environment:
      POSTGRES_USER: superadvocate
      POSTGRES_PASSWORD: superadvocate
      POSTGRES_DB: superadvocate
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U superadvocate -d superadvocate"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - superadvocate-network

  # Redis cache and Celery broker
  redis:
    image: redis:7.2.1-alpine
    container_name: superadvocate-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - superadvocate-network

  # One-shot migration runner — exits after migrations complete
  # Usage: docker-compose run --rm migrate
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
    command: python -m alembic -c backend/migrations/alembic.ini upgrade head
    working_dir: /app
    environment:
      - DATABASE_URL=postgresql+asyncpg://superadvocate:superadvocate@postgres:5432/superadvocate
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - superadvocate-network

  # One-shot dev user seeder — run once after migrate
  # Usage: docker-compose run --rm seed
  # Creates: dev@superadvocate.ai / devpassword123
  seed:
    build:
      context: .
      dockerfile: Dockerfile
    command: python scripts/seed_dev_user.py
    working_dir: /app
    environment:
      - DATABASE_URL=postgresql+asyncpg://superadvocate:superadvocate@postgres:5432/superadvocate
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - superadvocate-network

  # Celery worker for background jobs
  celery:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: superadvocate-celery
    command: celery -A celery_app:celery_app worker --loglevel=info
    environment:
      - ENVIRONMENT=development
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=redis://redis:6379/0
      - USE_AZURE_OPENAI=${USE_AZURE_OPENAI:-false}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_ORG_ID=${OPENAI_ORG_ID}
      - AZURE_OPENAI_API_KEY=${AZURE_OPENAI_API_KEY}
      - AZURE_OPENAI_ENDPOINT=${AZURE_OPENAI_ENDPOINT}
      - AZURE_OPENAI_API_VERSION=2024-10-21
      - GPT4O_DEPLOYMENT=${GPT4O_DEPLOYMENT:-gpt-4o}
      - GPT4O_MINI_DEPLOYMENT=${GPT4O_MINI_DEPLOYMENT:-gpt-4o-mini}
      - EMBEDDING_DEPLOYMENT=${EMBEDDING_DEPLOYMENT:-text-embedding-ada-002}
      - AZURE_SEARCH_ENDPOINT=${AZURE_SEARCH_ENDPOINT}
      - AZURE_SEARCH_KEY=${AZURE_SEARCH_KEY}
      - BLOB_CONNECTION_STRING=${BLOB_CONNECTION_STRING}
      - SENDGRID_API_KEY=${SENDGRID_API_KEY}
      - WHATSAPP_API_TOKEN=${WHATSAPP_API_TOKEN}
      - WHATSAPP_PHONE_NUMBER_ID=${WHATSAPP_PHONE_NUMBER_ID}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-dev-secret-key-change-in-production}
      - CELERY_WORKER=true
    volumes:
      - .:/app
    depends_on:
      - redis
      - postgres
      - backend
    networks:
      - superadvocate-network

volumes:
  postgres_data:
  redis_data:

networks:
  superadvocate-network:
    driver: bridge
